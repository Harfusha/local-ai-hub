# Local AI Hub update prompt

Update Local AI Hub with the repository's supported installer. Preserve the user's current configuration and unrelated agent instructions, then regenerate and merge the current Hub policy and companion skills.

Ensure the update refreshes the token-economizer skill, its dependencies, CLI wrappers, and the user's `PATH` entry for the Hub virtual-environment scripts directory. The command broker should allow the bundled read-only token-economy CLIs and `trim-run` only when it wraps an explicitly safe read/validation/build command. Keep `repomix` in stdout mode and keep mutating or unknown commands disabled by default.

Preserve model routing defaults: use installed `qwen2.5-coder:0.5b` only for preprocessing, `qwen2.5-coder:1.5b` for quick tasks, `qwen2.5-coder:3b` for complex tasks, and `qwen2.5-coder:7b` for the hardest reasoning. On Intel-only GPU systems, optionally use llama.cpp SYCL for all four aliases, as documented in `docs/LLAMA_CPP_SYCL.md`; keep auto-selection and Ollama fallback enabled unless the operator explicitly configures a verified SYCL router. NVIDIA/AMD discrete-GPU routes stay on Ollama CUDA/ROCm; AMD iGPU does not use Intel SYCL. Keep simple deterministic repository tasks on indexed or deterministic Hub actions; verify aliases against Ollama `/api/tags` or the llama.cpp router `/models` endpoint.

After the update, start a fresh shell or restart the agent host to pick up `PATH`, then verify with `tools/doctor.py`, `hubctl status`, and the repository's prescribed validation commands.

Regenerate the MCP action schemas and descriptions, `local-ai-orchestrator` skill and references, and managed agent instruction blocks from the effective feature configuration. Keep action enums and argument schemas complete, include trigger guidance for enabled specialized task, RAG, command, code-intelligence, and operator-dashboard capabilities, and omit disabled ones; expose `local_ai_work` only when `work_orchestrator` is enabled. When Agent OS is enabled, make its trigger and lifecycle explicit: use it for non-trivial multi-step, long-running, delegated, or acceptance-criteria work; create a task contract, checkpoint progress, compile context when needed, attach validation receipts, and gate completion with `verify_completion`. Reload the MCP server or restart the agent host so clients receive the regenerated schemas and descriptions.
